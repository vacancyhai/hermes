# Hermes Admin React Frontend — Environment Variables
# Relative path so browser calls go through Vite proxy
VITE_BACKEND_API_URL=/api/v1
# Proxy target (server-side only, Docker-internal)
VITE_PROXY_TARGET=http://backend:8000
